# Electron binary 下载镜像（npmmirror，避免 GitHub 直连失败）
# @electron/get 会读取 electron_mirror 配置，拼出
# https://registry.npmmirror.com/-/binary/electron/<version>/<artifact>.zip
electron_mirror=https://registry.npmmirror.com/-/binary/electron/
